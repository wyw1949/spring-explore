server:
  port: 8085
spring:
  application:
    name: config-client
  cloud:
    config:
      name: config-client
      profile: dev
      label: master
      uri: http://localhost:8086
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"